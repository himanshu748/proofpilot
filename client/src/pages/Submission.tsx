import ProofPilotLayout from "@/components/ProofPilotLayout";
import { Check, CircleDashed, Code2, ExternalLink, FileCheck2, MonitorPlay, Network, ShieldCheck } from "lucide-react";

const evidence = [{ name: "Nutrient DWS", state: "Core workflow", detail: "Data extraction with confidence scoring and finalization adapter are wired server-side. A live API key is required before recording live provider evidence." }, { name: "DevNetwork overall", state: "Eligible concept", detail: "Progress, real-world problem, and startup feasibility are demonstrated through the public workflow and submitted architecture." }, { name: "SerpApi", state: "Optional extension", detail: "A credible evidence-research feature can be added only after a SerpApi key is provided and calls are shown in the reviewer workflow." }, { name: "Doctavian", state: "Optional extension", detail: "A review certificate workflow can be added after credentials are provided; it is not represented as active in this build." }];

export default function Submission() { return <ProofPilotLayout><main className="mx-auto max-w-6xl px-4 py-8 sm:px-7 lg:px-10 lg:py-11"><div className="max-w-3xl"><div className="mb-3 inline-flex items-center gap-2 rounded-full bg-[#edf7f4] px-3 py-1.5 text-[10px] font-semibold uppercase tracking-[.14em] text-[#287063]"><FileCheck2 className="size-3" />Hackathon submission kit</div><h1 className="text-3xl font-semibold tracking-[-.045em] text-[#112a25] sm:text-4xl">ProofPilot makes document automation accountable.</h1><p className="mt-4 text-sm leading-7 text-slate-500">An AI-powered PDF processing workspace that extracts source-grounded data, exposes uncertainty to a human reviewer, retains the decision trail, and finalizes a document record only after review.</p></div><section className="mt-8 grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><article className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7"><p className="text-[11px] font-semibold uppercase tracking-[.14em] text-[#47776c]">One-line summary</p><p className="mt-3 text-xl font-semibold tracking-tight text-slate-800">From messy PDF to a decision you can defend.</p><div className="mt-8 border-t border-slate-100 pt-6"><p className="text-[11px] font-semibold uppercase tracking-[.14em] text-[#47776c]">Project pitch</p><p className="mt-3 text-sm leading-7 text-slate-600">Regulated teams cannot treat a document model’s answer as an approval. ProofPilot uses Nutrient DWS to turn a source PDF into confidence-scored fields, routes the uncertain values to a human, records every action in a durable audit trail, and prepares a tamper-evident final output after a reviewer approves it. The result is a transparent document pipeline that is designed for the moments when “almost right” is not right enough.</p></div></article><aside className="rounded-2xl bg-[#0c302c] p-5 text-white sm:p-7"><MonitorPlay className="size-5 text-[#a7ead8]" /><p className="mt-5 text-lg font-semibold">Demo video placeholder</p><p className="mt-2 text-xs leading-5 text-[#b7d4cc]">Record a 90–120 second walkthrough: upload the sample PDF, show the flagged term, correct it, show the audit entry, and explain the finalization request.</p><div className="mt-6 grid aspect-video place-items-center rounded-xl border border-dashed border-white/20 bg-white/[.06]"><div className="text-center"><span className="mx-auto grid size-10 place-items-center rounded-full bg-white/10"><MonitorPlay className="size-4" /></span><p className="mt-2 text-[11px] font-semibold text-[#c8e3dc]">Add public demo video URL</p></div></div></aside></section><section className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 sm:p-7"><div className="mb-6 flex items-start gap-3"><span className="grid size-9 place-items-center rounded-xl bg-[#eef6f3] text-[#287063]"><Network className="size-4" /></span><div><h2 className="text-sm font-semibold">Sponsor evidence matrix</h2><p className="mt-1 text-xs leading-5 text-slate-500">Only integrations that are product-critical and visible in the demo should be claimed in a submission.</p></div></div><div className="grid gap-3 md:grid-cols-2">{evidence.map(item => <div key={item.name} className="rounded-xl border border-slate-100 bg-[#fcfdfc] p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold text-slate-700">{item.name}</p><span className={`inline-flex items-center gap-1 rounded-full px-2 py-1 text-[10px] font-semibold ${item.state === "Core workflow" ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-500"}`}>{item.state === "Core workflow" ? <Check className="size-3" /> : <CircleDashed className="size-3" />}{item.state}</span></div><p className="mt-2 text-xs leading-5 text-slate-500">{item.detail}</p></div>)}</div></section><section className="mt-6 grid gap-4 sm:grid-cols-3">{[{ icon: ShieldCheck, title: "Trust by design", text: "Source citations, field confidence, review gates, and a timestamped audit log." }, { icon: Code2, title: "Built to prove", text: "A clean multi-route demo lets judges inspect the user experience and intended backend workflow." }, { icon: ExternalLink, title: "What remains", text: "Add a live Nutrient key, record the final demo video, then publish the approved submission." }].map(({ icon: Icon, title, text }) => <div key={title} className="rounded-2xl border border-slate-200 bg-white p-5"><Icon className="size-4 text-[#3b766a]" /><p className="mt-4 text-sm font-semibold text-slate-700">{title}</p><p className="mt-2 text-xs leading-5 text-slate-500">{text}</p></div>)}</section></main></ProofPilotLayout>; }
